// if ( a === 0 ) {
//     alert(`Верно`)
// } else {
//     alert(`Неверно`)
// }
        
// switch (a) {
//      case 0:
//         alert(`Верно`);
//         break;
//      default:
//         alert(`Неверно`);
// }
            
// a === 0 ? alert(`Верно`) : alert(`Неверно`);

    // 1 - 9

// let a = +prompt(`Введи число`);

// a === 0 ? alert(`Верно`) : alert(`Неверно`);
// a > 0 ? alert(`Верно`) : alert(`Неверно`);
// a < 0 ? alert(`Верно`) : alert(`Неверно`);
// a >= 0 ? alert(`Верно`) : alert(`Неверно`);
// a <= 0 ? alert(`Верно`) : alert(`Неверно`);
// a !== 0 ? alert(`Верно`) : alert(`Неверно`);
// a === `test` ? alert(`Верно`) : alert(`Неверно`);
// a === `1` ? alert(`Верно`) : alert(`Неверно`);
// a > 0 && a < 5 ? alert(`Верно`) : alert(`Неверно`);

    // 9 - 12   

// let b = +prompt(`Введи второй операнд`);
            
// a === 0 || a === 2 ? alert(a + 7) : alert(a / 10);
// a <= 1 && b >= 3 ? alert( a + b ) : alert ( a - b );
// a > 2 && a < 11 || b >= 6 && b < 14 ? alert(`Верно`) : alert(`Неверно`);

    // 13

// let num  =+prompt(`Введи время года`);

// switch (num) {
//     case 1:
//         num = `Winter`;
//         break;
//     case 2:
//         num = `Spring`;
//         break;
//     case 3:
//         num = `Summer`;
//         break;
//     case 4:
//         num = `Autumn`;
//         break;   
// }
// alert(num);




// let a = prompt(`Введи число`);
// console.log(a)
// let condition = a === null ? `Куда ушел?` :
// a > 5 ? `Ну значит это больше пяти` :
// a < 0 ? `Ну значит это меньше нуля` :
// a >= 6 ? `Ну это или шесть или больше` :
// a <= -1 ? `Ну это что то минусовое уже` :
// a === 0 ? `Короче это ноль` :
// a === `test` ? `Это что тест?` :
// a === `1` ? `Это единичка?` :
// a > 1 && a < 5 ? `Короче больше нуля, но меньше пяти` :
// `Ну в таком случае я безсилен`
// console.log(condition);
// alert(condition);

